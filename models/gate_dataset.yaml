# One-class Ultralytics detection dataset.
# Paths are relative to this YAML file. Expected repository layout:
# datasets/gates/
#   images/train/  images/val/
#   labels/train/  labels/val/
train: ../datasets/gates/images/train
val: ../datasets/gates/images/val

names:
  0: gate
